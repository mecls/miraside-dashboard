# Project / Topic
Internal **Meta Ads management dashboard for Miraside AI** (single FB ad account, EUR, Portugal). Next.js 16 + Supabase, deployed on Vercel, live at **https://miraside-dashboard.vercel.app** (login required). This session turned the old "Campaigns" page into a full **Ads Manager** (campaign→ad set→ad drill-down with per-row Charts/Edit/Duplicate + on/off toggles) and built out the **Ads Launcher** (the ad-creation tool). The GoHighLevel/revenue half is **parked — do not work on it.**

## STATUS (current — 2026-06-20)
The app is **fully built and live**. The Ad Launcher, Ads Manager, Overview dashboard, Settings, and Team/roles are all done, deployed, and mostly browser-verified. The one big unbuilt piece is the **GoHighLevel revenue/ROAS half — still parked; do NOT work on it** unless the user explicitly asks. Read this whole file (esp. HARD RULES + the dated update log just below), confirm understanding in 2–3 lines, then take the user's next request.

## NAV / PAGES (what's live)
- **Overview** (`/`) — KPIs + **weekly-leads** line chart + **daily-spend** bar chart + a date **range picker**.
- **Ads Manager** (`/campaigns`) — campaign → ad set → ad drill-down with per-row tools.
- **Ad Launcher** (`/launch`) — the **single, unified launcher** (creative-first grid + everything the classic launcher did). The classic `/create` was merged in and **removed** (2026-06-20).
- **Decisions** (`/decision`) — parked (GHL-dependent).
- **Settings** (`/settings`) and **Team** (`/team`) — admin-gated (see Roles).

## LAUNCHER MERGE — 2026-06-20 (classic `/create` folded into `/launch`, then deleted)
The two launchers are now **ONE**. Everything classic did was ported into the creative-first `/launch` grid; the classic page + `components/CreateAdForm.tsx` + `app/api/ads/create` were **deleted** and the nav "classic" tab removed (`/create` → 404). What landed:
- **Duplicate existing ads** — import-screen entry → `components/launcher/DuplicateModal.tsx`: **Exact copies** (native `/copies` via `copyObject`, `app/api/ads/copy`) and **New creative** (recompose: reuse the source's ad set + budget + lead form, new image+copy, via `app/api/ads/duplicate`). All paused.
- **Editable audience + presets** — the "Launch New" panel opens an **Audience & settings** modal (`adsetup/LaunchSettingsModal.tsx`): preset apply/save/delete (`ad_presets` + new `extra` jsonb; `/api/presets` POST+DELETE), countries (searchable, multi)/age (slider)/gender/FB-IG/Advantage+, a **destination** (instant form vs landing page), and an **Advanced** section (placement groups, optimization goal, attribution window). Defaults = the proven setup. As of 2026-06-20 this modal edits ONE of possibly **several audiences** (see LAUNCHER UPGRADES above). `lib/launch.ts` `buildTargeting(cfg)` is now audience-driven (was hardcoded `lockedTargeting()`); helpers in `components/launcher/audience.ts`.
- **Lead-form builder** — the grid's lead-form picker has a **"➕ Build a new form…"** option → `adsetup/LeadFormBuilderModal.tsx` (custom qualifying questions + Full name/Phone/Email + thank-you URL), saved to `lead_form_templates` via `app/api/lead-forms` (shared `lib/leadform.ts normalizeQuestions`); Meta form minted on launch. Lead Form is now a default-visible column.
- **All-placements preview** — click a row's media → `adsetup/PreviewModal.tsx` renders the 10 FB/IG surfaces via `app/api/ads/preview`. Verified rendering 10 iframes (real image); also surfaces Meta's error for bad images.
- **UTM wired** — the row's UTM now appends to the destination link (`withUtm` in `lib/launch.ts`); was captured-but-dropped.
- Verified live (browser): duplicate UI (both modes), settings/presets/advanced, lead-form builder (DB write, correct normalized questions, no Meta object), preview (10 placements). Meta-write paths reuse already-proven helpers — first real click is the live test (all paused).

## LEAVE-SETUP NAV GUARD — 2026-06-22
The Ad Setup sheet now guards against losing work on navigation. While `AdSetup` is mounted, a document-level **capture-phase click listener** intercepts in-app nav-link clicks (sidebar tabs etc.) — `e.preventDefault()` + `stopPropagation()`, stores the intended href in `pendingHref`, and shows the existing **"Leave Ad Setup?"** prompt instead of navigating. The prompt's actions honor the destination: **Continue editing** stays; **Discard** → `router.push(pendingHref)`; **Save as draft** → `saveDraft(pendingHref)` (saveDraft now takes an optional `nextHref` and `router.push`es there on success instead of `onExit()`). A `beforeunload` handler covers hard reloads / tab close (generic browser warning). Back/Close buttons still use the same prompt with `pendingHref=null` (→ `onExit()` to the launcher front page). Verified live: sidebar click → prompt (URL stays /launch); Continue stays; Discard → /settings; Save as draft → saves + → /team.

## AUTO-NAMING ADS FROM THE IMAGE — 2026-06-22
**UPDATE 2026-07-19 — STYLE TAG REMOVED (user order):** a later iteration had the model also return a one-word style tag (`Text`/`Design`/`Photo`) used to disambiguate same-copy creatives ("Headline — Text" vs "Headline — Design"). Miguel had it removed completely. Now: the model returns the NAME ONLY; duplicate generated names are numbered ("Headline", "Headline 2", …). Do not reintroduce the style tag without an explicit ask.
When you import creatives (or "+ Add Ads"), each image ad's **name is auto-generated from the image's text** by a vision model. New endpoint `app/api/ads/name-from-image/route.ts` (auth-gated, stores nothing): a downscaled JPEG is sent to **Anthropic `claude-haiku-4-5-20251001`** (key `ANTHROPIC_API_KEY`, already set) with a prompt to read the headline and return a short 1–5-word Title-Case name; output is sanitized (strip quotes/punctuation, cap 6 words/60 chars). Client (`components/launcher/AdLauncher.tsx`): `imageToSmallBase64()` (canvas downscale to ≤1024px), `autoName(rows, pool)` runs after `goToAdSetup` + `addAds` with a concurrency cap of 4, **non-blocking** (table opens with the filename; names fill in as they return) and **never clobbers a name the user already edited** (`r.name === original` guard). A naming spinner shows in the Ad Name cell while generating (`AdSetup` `namingIds` prop). Video rows + draft-resume are skipped. **NOTE:** this is distinct from the **"Write with AI" copy generator that was CUT** (see new-ad-launcher-build memory's 2026-06-18 decision) — that was for ad COPY and stays cut; auto-naming is a separate, explicitly-requested feature. Verified live: a test "Grow Without Hiring" creative → name auto-set to "Grow Without Hiring".

## LEAD FORM BUILDER — 2026-06-21 (expanded; privacy + thank-you moved to Settings)
The instant-form builder (`components/launcher/adsetup/LeadFormBuilderModal.tsx`) was expanded:
- **Greeting (intro)** per form: headline + a paragraph OR a bullet list (≤5). Stored in a new `lead_form_templates.greeting` jsonb; normalized to Meta's `context_card` ({title, style: PARAGRAPH_STYLE|LIST_STYLE, content[], button_text}) at launch.
- **Question types**: Multiple choice (`CUSTOM` + options), Short answer (`CUSTOM`, no options = free text), **Appointment request** (Meta `DATE_TIME` — lead picks a preferred date/time; there is no dedicated APPOINTMENT type). Conditional logic intentionally not built. `lib/leadform.ts` `normalizeQuestions` handles DATE_TIME; `normalizeGreeting` builds the context_card.
- **Form type = "More volume"** always (it's Meta's default — higher-intent would add a review step, which we don't send); no rich creative; no flexible delivery.
- **Privacy + the after-submit ("thank you") screen moved to Settings** (`/settings` → "Lead form defaults", `components/LeadFormDefaults.tsx`): privacy policy link, thank-you **headline + message ("message for leads") + Go-to-website link + button text**. Set once; every form reuses them. Stored in a NEW table **`lead_form_settings`** (one row per tenant; cols privacy_url/thank_you_headline/thank_you_message/thank_you_url/thank_you_button; RLS-on, service-role only) — NOT `tenant_settings` (its `validate_tenant_setting` trigger rejects keys absent from `setting_definitions`). Helper `lib/lead-defaults.ts getLeadFormDefaults(admin, tenantId)`; save via admin-gated `app/api/settings/lead-form`.
- At launch, `lib/launch.ts resolveLeadFormMetaId` combines the template (questions + greeting) with the Settings defaults (privacy + thank_you_page VIEW_WEBSITE) → `createLeadForm` (`lib/meta-ads.ts`, now takes `contextCard` + `thankYou` + `privacyLinkText`). The Meta form is still minted lazily on first launch (the live test).
- Verified live (2026-06-21): Settings save → `lead_form_settings` row written; builder → `lead_form_templates` row with correct greeting + normalized questions (CUSTOM+options / CUSTOM / DATE_TIME / FULL_NAME / PHONE). Did NOT launch (no Meta form created). NOTE: a stray pre-existing template "ZZZ DB test" remains in the library (not from this work).

## LAUNCHER UPGRADES — 2026-06-20 (multi-audience, landing pages, CBO/ABO, polish)
Built on the merged `/launch`. All **PAUSED**; all deployed + browser-verified (display-level; first real launch is the live test).
- **Multiple audiences per launch (Thing 1).** "Launch New" now holds an **Audiences** list — add / edit / duplicate / delete, each its own targeting → its own PAUSED **ad set** in the campaign. A per-row chip picker (`adsetup/cells.tsx AudienceCell`) assigns each ad to specific audiences (empty = all; ≥1 always kept). Types: `AudienceSet` + `AdRow.audienceIds` (`components/launcher/types.ts`); helpers `makeAudienceSet`/`adSetNameFor` (`audience.ts`). Backend `lib/launch.ts prepareNewAdSets` rewritten: one ad set per audience (plain ads share an audience's regular ad set; asset-feed ads get a dedicated dynamic ad set per audience), ads emitted **one work item per (ad × destination)** so the right creative is built per ad set. `AudienceConfig[]` on `NewAdSetConfig`; parsed/validated in `app/api/launches/create/route.ts`. Drafts persist `audiences` + per-row `audienceIds` (old single-audience drafts auto-migrate; rows without `audienceIds` normalized on resume).
- **Landing-page destination (Thing 2).** Per-audience **"Where the ad sends people": Instant form vs Landing page** (`LaunchSettingsModal`). Landing page = a URL + the ad set optimizes for **website conversions** (`OFFSITE_CONVERSIONS`, `destination_type WEBSITE`, `promoted_object {pixel_id, custom_event_type:"LEAD"}`) using the account pixel auto-detected by `lib/meta-ads.ts getAccountPixelId()` (cached; clear error if no pixel). The creative links to the URL with **no lead form**. **No pixel-setup UI, no qualification logic, no CAPI** — qualification + the landing page itself live in **GoHighLevel**; conversions should fire only on qualified prospects (handled there). Panel/chips show form (green dot) vs landing page (blue dot).
- **CBO vs ABO** is selectable in the "Launch New" panel (campaign-level budget vs per-ad-set budget). `createCampaign`/`createAdSet` already supported both; the toggle wires `budgetMode` through `prepareNewAdSets` (ABO sets the budget + a start_time on each ad set).
- **Audience modal upgrade.** Country picker is now a **searchable list of ~80 countries** (`lib/countries.ts`) with removable chips; age is a **dual-thumb slider** (`app/globals.css .range-thumb`); the unused Start/End **schedule fields were removed**.
- **Launch progress bar** (`adsetup/LaunchProgress.tsx`) + consistent **loading overlays/spinners** on every async wait (launch, save draft, duplicate, preview, resume, preset/lead-form saves) — no more dead clicks.
- **Launch History**: duplicate launches now **log** to `ad_launches` (both `/api/ads/copy` + `/api/ads/duplicate`), and each row has a minimal hover **✕ to delete** the history record (never touches live Meta ads; drafts also clear their Storage media) via `DELETE /api/launches/[id]`.
- **Launch History draft interaction (changed 2026-06-20).** Clicking a row now opens a **view-only image-preview popup** (`components/launcher/LaunchPreviewModal.tsx`) showing the ad image(s) **bigger** so you can confirm it's the right one — for drafts it fetches fresh signed URLs via `GET /api/launches/[id]?images=1` (new `images=1` branch signs each image creative's Storage path; falls back to the row's stored thumbs). The **"Open in editor →"** action moved off the row text onto a **hover-revealed button** in the actions cell (next to the ✕ delete), so: click row = preview; hover → "Open in editor →" = resume into the spreadsheet to launch. Only drafts get the open button; the popup works for any row with images. Verified live (made a draft → row-click popup shows the image large → hover button opens the grid).
- **Duplicate now OPENS THE EDITABLE GRID (changed 2026-06-20).** The "Duplicate existing ads" → **Exact copies** flow no longer creates ads directly. Selecting ads → **"Open in editor"** loads them into the Ad Setup grid **pre-filled** (image downloaded from Meta + name + copy/headline + link + CTA + kept lead form + source ad set pre-selected in Existing mode), so the user tweaks a thing or two — and can use the new multi-audience / landing-page / CBO-ABO options — then **Launch** creates fresh PAUSED ads. New endpoint `app/api/ads/duplicate-load/route.ts` (READS each ad via Meta + downloads the creative image as a data URL; image creatives only — videos/unsupported reported as `skipped`; CTA clamped to a known option). Kept lead form rides through as a **`meta:<formId>`** sentinel (`launchAds` uses it directly, skipping the local-template lookup); the grid shows it as "<name> (kept)". Wiring: `AdLauncher.openDuplicatesInEditor` (decodes images → `UploadedCreative` + rows → opens grid, merges the kept form into `data.leadForms`); `DuplicateModal` exact mode calls `onOpenInEditor`. **UPDATE 2026-06-21: New creative (recompose) ALSO opens the editor** (`AdLauncher.openRecomposeInEditor` → loads the source's setup via `/api/ads/duplicate-load` **with `setupOnly:true`** (skips the source image download — recompose supplies its own new image; without this flag a source ad whose Meta image URL won't fetch server-side returned a 400 and the editor never opened, the bug reported 2026-06-21), builds a row with the user's NEW image + copy + the source ad set pre-selected + kept lead form). The recompose "Open in editor" button is disabled until a new image is added. NOTE: all toasts in the app are GREEN (`components/Toaster.tsx` has no error variant), so an error toast looks like success. The modal's whole "Where to?" destination block (incl. the confusing one-preset "Audience preset" dropdown) was REMOVED from recompose — full destination/audience control now lives in the editor for BOTH modes. The old direct-create paths `/api/ads/copy` + `/api/ads/duplicate` are now UNUSED (kept in repo). Verified live: exact (2 ads → editor pre-filled) and recompose (source ad set "New Leads Ad Set" pre-selected + new image + new copy); did NOT launch.

## LATEST UPDATES — earlier 2026-06 sessions (newest first)
**1. New creative-first Ad Launcher (`/launch`, `components/launcher/*`).**
Flow: drop creatives (`ImportZone`) → auto-group by aspect ratio (`GroupingModal` + `ratio.ts`) → choose format (`ChooseFormatModal`: single image, single video, or **carousel** via `CarouselModal`) → an editable **spreadsheet-style ad grid** (`adsetup/AdSetup.tsx`) with per-row name/primary-text/headline/CTA, **bulk edit** (`BulkEdit.tsx`), a **column picker** (`ColumnPicker.tsx`), and inline cells (`cells.tsx`). Everything launches **PAUSED** (the grid's status cell is a static amber "Paused" pill). Backed by the `ad_launches` table + `app/api/launches/{route,create,upload-video,[id]}`; `lib/launch.ts` builds the Meta objects.
   - **Multi-ratio (3+ distinct ratios):** `multiRatioBuckets()` picks vertical (min ratio) / feed (closest to 0.8) / wide (max ratio) buckets and builds `asset_customization_rules` so the right crop shows per placement; if it can't customize, it falls back to a **dynamic-creative** ad set. Enhancements (`assetFeedFeatures`) apply deterministically to every creative.
   - **Large-video upload:** `lib/meta.ts uploadAdVideoResumable()` chunk-uploads videos > 8 MB (start/transfer/finish) via Supabase Storage `launch-media`; smaller videos use the simple path.
   - **Resumable drafts:** an in-progress launch saves to `ad_launches` (media + signed-URL thumbnails + full grid/campaign/budget state). Reopening from `LaunchHistory` rebuilds the media and restores all state; re-saving **overwrites the same draft row** (no duplicates). Verified end-to-end in the browser.

**2. Roles & access (admin vs user)** — see the **ROLES & ACCESS** section below. Team tab renamed **"People with access" → "Users"**; per-user admin/user toggle; admins can't be deleted; **users can view but not change** Settings or the Team (UI disabled + server 403); only admins can change roles.

**3. Overview dashboard charts + range picker.**
   - **Weekly-leads chart** (`components/charts/LeadsLine.tsx`, data built in `lib/queries.ts`): a **continuous, zero-filled** weekly series across the selected range (empty weeks render as 0, so a 0→N trend is visible), counting **complete Mon–Sun weeks**; the in-progress current week is flagged "partial" (hollow dot, "(in progress)" tooltip). Spans the **same window as the daily-spend chart**.
   - **Range picker** (`components/RangePicker.tsx`): redesigned from a row of ~8 buttons into **one clean dropdown**; added **This week** (Mon → today) and **Last week** (prev Mon → Sun) alongside 7/30/90-day, this/last month, this/last year, and Custom (calendar). Anchored on account-local "today". Used on every page that has a range.

**4. Robustness.** Meta **rate-limit backoff** (`lib/meta.ts withRetry`, exponential retry ~1.5/3/6 s on codes 4/17/32/341/613/80004) wraps all Meta calls. **Sync reconciliation:** `runFacebookSync` now also **deletes** local rows for campaigns/ad sets/ads no longer present on Meta (FK cascade), so turning ads off / deleting them in Meta is reflected here (it was upsert-only before, which left ghost rows showing "on").

**5. Deploy/build safety (learned this session).** `npx tsc --noEmit` does **not** catch all JSX/parse errors that the production **Turbopack** build catches. The Vercel build is fail-closed (a broken build won't go live), so the real rule is: **never let a deploy proceed when typecheck/build fails** — don't chain `vercel --prod` off a piped `tsc` whose exit code is masked by `| head` (that bug shipped a broken build briefly this session). To catch parse errors pre-deploy, run `npm run build` locally **after stopping `npm run dev`** (they share `.next`, so building while dev runs corrupts it). (A stray `{/* … */}` JSX comment in `SettingsForm.tsx` was the culprit; fixed with a `//` comment.)

## ROLES & ACCESS (admin vs user)
- `lib/admin.ts`: `isAdminUser(u)` = bootstrap **`ADMIN_EMAIL`** (default miguel.v.rolo@gmail.com, env-overridable) **OR** Supabase `app_metadata.role === "admin"`. The bootstrap email is **always admin — can't be demoted/removed** (anti-lockout).
- **Team** (`/team`, `components/TeamManager.tsx`): admins add users, toggle **Make admin / Make user** (PATCH `/api/users` sets `app_metadata.role`), and remove. **Admins can't be removed** (demote to user first). New users start as "user". Non-admins see the list **read-only**.
- **Settings** (`/settings`): users can **view but not change** — every control is `disabled` + a read-only notice (`SettingsForm` takes `canEdit`), and `POST /api/settings` returns **403** for non-admins (real server enforcement, not just hidden UI).
- `/api/users` (POST add / PATCH role / DELETE) is **admin-only** (403 otherwise) — a "user" can't self-escalate. Verified live as both an admin and a user.

## Live app + DEPLOY WORKFLOW (critical — read before deploying)
- Working dir: `/Users/miguelrolo/Desktop/FB Ads Dashboard` (NOT a git repo). `npm run dev` is usually already running on :3000 — do **NOT** `npm run build` while dev runs (corrupts `.next`). Use `npx tsc --noEmit -p tsconfig.json` to typecheck.
- Deploy: `vercel --prod --yes` (CLI authed as `miguelvieirarolo`). The CLI sometimes dies with `read ETIMEDOUT` while polling but the build still succeeds — verify with `vercel ls` / `vercel inspect`.
- **GOTCHA (always do this):** `vercel --prod` does NOT update the user's URL. After every deploy you MUST run:
  `vercel alias set https://<new-deployment-url> miraside-dashboard.vercel.app`
  then verify with `vercel inspect https://miraside-dashboard.vercel.app | grep created` (should be fresh). The project's default domain is `fb-ads-dashboard-ten.vercel.app`; `miraside-dashboard.vercel.app` is a manual alias that does not auto-follow production.
- Supabase MCP is connected (project ref `sybpedxhmbalfzvntzcd`). `apply_migration` / `execute_sql` work directly.

## HARD RULES (do not break)
- **Never change ACTIVE/PAUSED status** of any campaign/ad set/ad yourself — only the user flips toggles. Approval to edit budget/targeting/creative is NOT approval to change status; ask per-action. (The dashboard's toggles/confirms handle this; you never call resume/pause.)
- **Verify writes against the LIVE Meta API only with throwaway PAUSED objects, then delete/archive** — and the user is sensitive about creating ANY objects on the real account (the safety classifier has blocked it). When in doubt, don't self-test creation; the user's own click is the live test (safe because everything is paused). Clean up `scripts/_*.ts` temp scripts.
- **Be concise, recommend (don't present menus), explain jargon plainly** (user is non-technical — explain in everyday terms, no API/enum-speak). **Push back when something's wrong.** Report honestly what's verified vs not.
- Confirm before deploying outward-facing changes; the user has been approving each deploy.

## THE AD LAUNCHER (`/launch`) — the only launcher now
The classic `/create` was **merged in and deleted** (see "LAUNCHER MERGE" above). `/launch` (`components/launcher/*`) does everything: creative-first import → group/format → spreadsheet ad grid (single/video/carousel/multi-ratio/flexible) → launch **PAUSED**; plus duplicate-existing-ads, editable audience + presets, the lead-form builder, all-placements preview, bulk edit, column picker, and resumable drafts.
- Entry: `app/(app)/launch/page.tsx` loads launches, lead forms, **existing ads** (for Duplicate), **ad_presets**, and the live campaign→ad-set tree. Renders `components/launcher/AdLauncher.tsx`.
- Launch API: `app/api/launches/create/route.ts` → `lib/launch.ts launchAds()` (status hard-coded PAUSED; `buildTargeting(cfg)` audience-driven; `prepareNewAdSets`). Drafts: `app/api/launches/{route,[id]}`. Video: `app/api/launches/upload-video`.
- Supporting routes (shared with the merged features): `app/api/ads/copy` (exact duplicate), `app/api/ads/duplicate` (recompose), `app/api/ads/preview` (placement previews), `app/api/presets` (preset save/delete), `app/api/lead-forms` (form builder save).

## WHAT THE ADS MANAGER DOES TODAY  (`/campaigns`, page renamed "Ads Manager")
`components/AdsManagerView.tsx`: tabbed drill-down **Campaigns → Ad sets → Ads**, each a table with an **on/off toggle** (`StatusToggle`), inline **rename** (`EditableName`), and a per-row hover action group (`RowActions`): **Charts** (deep-links to the real FB Ads Manager *insights* view via `lib/adsmanager.ts`), **Edit (+Publish)**, **Duplicate** (Meta `/copies`, forced paused twice — `copyObject`). On the Ads tab: KPI strip + best-creatives + the full `AdTable` (metrics/flags/drawer); ad-row actions sit under the ad name. Edit panels: `EditPanel.tsx` (campaign budget; ad set age/advertiser/FB-IG/placements) and `AdEditPanel.tsx` (ad: rename · replace image · **auto-crop toggle** · keep/swap saved form · archive form · enhancements always-on · all-placements preview). Success **toasts** bottom-right (`components/Toaster.tsx`, helper `toast("…")`). Settings prefill via `app/api/ads/settings/route.ts`; writes via `app/api/ads/manage/route.ts` actions: `pause|resume|archive|budget|rename|duplicate|edit_adset|edit_ad|archive_form`.

## KEY FILES
- Ad Launcher (`/launch`): `app/(app)/launch/page.tsx`; `components/launcher/{AdLauncher,ImportZone,ChooseFormatModal,GroupingModal,CarouselModal,DuplicateModal,LaunchHistory,audience.ts,ratio.ts,types.ts}`; grid `components/launcher/adsetup/{AdSetup,BulkEdit,ColumnPicker,LaunchSettingsModal,LeadFormBuilderModal,PreviewModal,cells,constants,Select,...}`; `lib/{launch,leadform}.ts`; routes `app/api/launches/{create,upload-video,route,[id]}`, `app/api/ads/{copy,duplicate,preview}`, `app/api/presets`, `app/api/lead-forms`.
- Write layer: `lib/meta-ads.ts` — `createCampaign / createAdSet / createLeadForm / archiveLeadForm / uploadAdImage / createAdCreative(+degreesOfFreedom) / createAd / setStatus / setBudget / updateObject / deleteObject / copyObject / generateAdPreview / creativeFeatures(autoCrop) / getAdCreativeInfo / listCampaignsWithAdSets / pageAccessToken`. Base API: `lib/meta.ts` (`metaGet/metaGetAll/metaPost/metaDelete`, `adAccountId`, `pageId`; `META_API_VERSION` default `v23.0`).
- Helpers: `lib/placements.ts` (group→enum mapping), `lib/adsmanager.ts` (deep links), `lib/tenant.ts` (`getPrimaryTenantId`), `lib/supabase/{admin,server,client}.ts`.
- Ads Manager UI: `components/{AdsManagerView,AdTable,RowActions,EditPanel,AdEditPanel,StatusToggle,EditableName,Toaster,Nav}.tsx`.
- Sync: `lib/sync/facebook.ts` (`runFacebookSync` — upserts insights AND **reconciles**: deletes local rows for objects no longer on Meta), `scripts/sync-facebook.ts` (`npm run sync:fb`); n8n triggers `POST /api/sync/facebook` every 30 min (see `n8n/README.md`).

## DB (Supabase, all RLS `tenant_id = current_tenant_id()`)
Existing 17 tables (campaigns, adsets, ads, fb_insights_*, ghl_*, setting_definitions[45], tenant_settings, …). Launcher tables: `lead_form_templates` (id, tenant_id, name, questions jsonb, follow_up_url, privacy_url, meta_form_id) and `ad_presets` (id, tenant_id, name, countries[], age_min, age_max, genders[], advantage_audience, publisher_platforms[], default_cta, default_form_template_id, **`extra jsonb`** ← placements/optimization/attribution/schedule). One seeded preset: "Portugal · 29–65 · leads (default)" = the launcher's default audience.

## VERIFIED META API KNOWLEDGE (researched + adversarially verified this session — trust these)
- **Limited spend on excluded placements:** via the API it defaults **OFF** ("advertisers are not defaulted into limited spend"); it's an opt-in field `placement_soft_opt_out` that **we never set** → placements we leave out are fully excluded (zero spend). In the Ads Manager UI the box defaults ON, so warn the user only if they edit placements directly in Facebook.
- **Placement enums — WRITE-DEPRECATED (in the docs list but REJECTED on write):** `video_feeds`, `facebook_reels_overlay`, `profile_reels`. Do NOT send them. Write-safe set used in `lib/placements.ts`: Feeds=FB[feed,profile_feed,marketplace] IG[stream,profile_feed]; Stories=FB/IG[story]; Reels=FB[facebook_reels] IG[reels]; In-stream=FB[instream_video] IG none.
- **Creative enhancements:** the umbrella `standard_enhancements` bundle is **deprecated (v22+) and rejected on create** — use individual `degrees_of_freedom_spec.creative_features_spec` keys (image_touchups, text_optimizations, inline_comment) each `OPT_IN`. Auto-crop = `adapt_to_placement` `OPT_IN`(crop per placement)/`OPT_OUT`(as-is). `creativeFeatures(autoCrop)` builds this.
- **Creatives are immutable** — editing an ad's image/form = build a NEW adcreative + `POST /{ad-id}{creative:{creative_id}}`; this re-submits the ad for review (harmless while paused).
- **Duplication** = Meta `/copies` (`deep_copy` for campaign/ad set; `status_option=PAUSED`).
- **Ads Manager deep-link** = `/{campaigns|adsets|ads}/insights?act=<num>&business_id=329549975846251&selected_*_ids=…`.

## LOCKED PRESET / DECISIONS (do NOT re-litigate)
- **Single ad account forever** (user decision) → no multi-tenant; the id-only lookup in `/api/ads/manage` + `/api/ads/duplicate` is a permanent non-issue (WON'T-FIX). Don't build tenant scoping.
- Ad preset: objective **OUTCOME_LEADS**, **CBO or ABO** (selectable; default CBO), LOWEST_COST_WITHOUT_CAP, **LEAD_GENERATION / ON_AD** (instant form) or **OFFSITE_CONVERSIONS / WEBSITE** (landing page), promoted Page (or pixel for landing pages), PT home+recent, **age 29–65** default, **Advantage+ Audience OFF**, **Facebook+Instagram** only, CLICK_THROUGH 1d, FACEBOOK_RELAXED. **Budget is always set per ad** (not in the preset). **All created PAUSED.**
- **Advertiser** (EU DSA `dsa_beneficiary`) default **"Miguel Rolo"**, option **"Miraside AI"** (payer stays separate).
- **Essential/standard enhancements always ON**; **auto-crop** is a user toggle.
- Two lead methods (per audience): **instant form** (default) and **landing page** (website conversions on the account pixel, event=Lead). The landing page + lead **qualification** live in **GoHighLevel**; we build "as if the pixel is set up" — no pixel-setup UI / no CAPI here (conversions fire only on qualified prospects, handled in GHL).
- Brand SVGs: never hand-retype; `cp` from `/Users/miguelrolo/Desktop/Miraside AI/Miraside AI Assets/`, render inline via `components/Brand.tsx`.

## KNOWN GAPS (current)
- **Lead-form builder can't load/clone an existing form's questions** for editing — build a new form or reuse a saved one as-is; editing a saved template isn't possible yet.
- **Facebook-page / Instagram / WhatsApp grid columns are inert** — single FB page only (hardcoded `META_PAGE_ID`); `data.instagram`/`data.whatsapp` are always empty, so those columns render but change nothing. (UTM is now wired through to the link.)
- **Advanced schedule** start/end convert browser-local time → ISO (fine for the PT user; not account-tz-aware). **QUALITY_LEAD** optimization is offered but unverified against on-ad instant forms — LEAD_GENERATION is the proven one.
- Cosmetic/optional (flagged, not done): hide Settings/Team from non-admins in the nav (currently visible read-only); unify chart date-label format (daily "06-18" vs weekly "Jun 8").
- (Ask the user for their specific list — they usually have one.)

## HONEST CAVEATS (verification status)
- **Real launches were tested live in an earlier session** via `/launch` — image and video ads were actually created on Meta (all **PAUSED**), confirmed, then deleted/cleaned up. The merge session's new features (duplicate/settings/lead-form/preview) were UI-verified in the browser; their Meta-write paths reuse already-proven helpers (`copyObject`, `/api/ads/duplicate`, `generateAdPreview`, `launchAds`). Self-testing object creation on the real account is blocked by the safety classifier — the user's first real click is the live test (all paused).
- The **Ads-Manager write buttons** (Edit-publish / Duplicate / on-off) were verified to render, drill down, and read correctly, but their **writes were NOT executed on the real live campaign** (user-sensitive; on/off would spend money). Built from verified API shapes; the user's first real click is the live test (safe while paused). Editing targeting/creative re-submits ads for review (harmless while paused).
- The original 8 ads are single-image (text baked into the image, so FB text fields are empty), stored as `asset_feed_spec` with a per-placement crop; the Ad-Edit rebuild flattens them to a single-image `link_data` creative (the auto-crop toggle replaces the static crop). Acceptable per user.

## IDENTIFIERS (preserve verbatim; secrets live in `.env`, never echo)
`META_AD_ACCOUNT_ID=act_1554168232873140` · `META_APP_ID=1031037082992608` (Live) · `META_PAGE_ID=1100260643178381` (Page "Miraside AI", IG `17841400501996300`) · FB business_id `329549975846251` · token = system user "Dashboard_Sync" (never expires, all ads scopes) · Supabase ref `sybpedxhmbalfzvntzcd`. Real objects (all PAUSED): campaign "New Leads Campaign" `120243244598270669`, ad set "New Leads Ad Set" `120243244598280669` (now age 29–~52, FB+IG, manual placements, Advantage off, dsa_beneficiary "Miguel Rolo"), 8 ads, lead form `3904314959876823` ("Formulário Teste 1" — real PT qualification form, 2 leads). Account: EUR, Europe/Lisbon, VISA funding, no spend cap, ~€35 spent.

## MY PREFERENCES / WORKING STYLE (the user)
Concise; recommend don't menu; **explain everything in plain language** (caught the assistant several times for jargon/over-explaining); push back when wrong; don't over-engineer; verify honestly; clean up test objects; intuitive UI (green/amber/red, clear buttons, success toasts). Builds are shipped phase-by-phase with a deploy after each.

## PROMPT FOR THE NEXT CLAUDE
```
Picking up Miraside AI's Meta Ads dashboard (Next.js 16 + Supabase, live at https://miraside-dashboard.vercel.app). Read HANDOFF.md at the project root (/Users/miguelrolo/Desktop/FB Ads Dashboard) and the auto-memory, and follow them. STATE: the app is fully built and live (Overview dashboard, Ads Manager, the new creative-first Ad Launcher at /launch, classic launcher at /create, Settings, Team/roles). The one big unbuilt piece is GHL revenue/ROAS — PARKED, do not touch it unless asked. Treat "Verified Meta API knowledge", "Locked preset / decisions", and the identifiers as fixed; preserve IDs/paths verbatim. CRITICAL deploy steps: make sure typecheck passes and never deploy on a failed build (tsc alone misses some Turbopack parse errors — for those, `npm run build` after stopping the dev server); after `vercel --prod` you MUST `vercel alias set <deployment-url> miraside-dashboard.vercel.app` or the user's URL won't update. HARD RULE: never change any ad's on/off status yourself; the user is sensitive about creating objects on the real account, so don't self-test creation against live Meta. Be concise, explain in plain non-technical language, push back when something's wrong. Confirm understanding in 2–3 lines, then ask the user what they'd like to work on next.
```
